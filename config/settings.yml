pagination:
  default_per_page: 25
  max_per_page: 100

jwt:
  expiration_hours: 24

rate_limit:
  login:
    requests: 5
    period_seconds: 300

cors:
  allowed_origins:
    - http://localhost:3001
